export class Image {
    public height: number;
    public url: string;
    public width: number;

    constructor(height: number, url: string, width: number) {
        this.height = height;
        this.url = url;
        this.width = width;
    }
}